
sudo apt install zsh mlocate bat git curl xdg-utils ripgrep exa libnotify-bin     -y



[ -f /etc/bat ] && mv /etc/bat /etc/bat.old 
sudo mv bat /etc/

[ -f /etc/zsh ] && mv /etc/zsh /etc/zsh.old 
mv etc /etc/

curl -sS https://starship.rs/install.sh | sh
